# 进度快照 017 - 新版本安装包编译与远程协助回归验证完成

## 本次已完成

1. 完成新版本安装包编译
   - 使用项目约定 Flutter：
     - `D:\APPdata\flutter`
   - 成功执行：
     - `scripts/export_installer_package.ps1`
   - 成功产出 `2.3` 版本便携包与安装包。

2. 完成编译前备份
   - 新增备份目录：
     - `D:\Myproject\vnt2.0\VntcApp1.0\backup\021_安装包编译验证前备份_20260601_133355`
   - 已按规则仅保留最近 10 份备份。

3. 完成安装包产物输出
   - 安装包：
     - `D:\Myproject\vnt2.0\VntcApp1.0\dist\installer\VNT_App_2.3_Windows_Setup.exe`
   - 安装包校验文件：
     - `D:\Myproject\vnt2.0\VntcApp1.0\dist\installer\VNT_App_2.3_Windows_Setup.sha256`
   - 便携包目录：
     - `D:\Myproject\vnt2.0\VntcApp1.0\dist\portable\VNT_App_2.3_Windows_Portable`
   - 便携包压缩文件：
     - `D:\Myproject\vnt2.0\VntcApp1.0\dist\portable\VNT_App_2.3_Windows_Portable.zip`

4. 完成远程协助制品接入校验
   - 便携包内已确认带有：
     - `remote_assist\artifacts\vntcrustdesk.msi`
     - `scripts\bootstrap_vntcrustdesk.ps1`
     - `scripts\uninstall_vntcrustdesk.ps1`
   - 安装包 staging 目录内已确认带有：
     - `vntcrustdesk.msi`
     - `bootstrap_vntcrustdesk.ps1`
     - `uninstall_vntcrustdesk.ps1`

5. 完成远程协助密码链路回归验证
   - 对 `vntcrustdesk.exe --configure-access-password abc123` 做了真实环境可回滚烟测，退出码为 `0`。
   - 对 `vntcrustdesk.exe --configure-access-password ""` 做了真实环境可回滚烟测，退出码为 `0`。
   - 已观察到：
     - 设密码后 `VNTC RustDesk.toml` 的 `password` 字段写入加密值。
     - 清密码后 `VNTC RustDesk.toml` 的 `password` 字段恢复为空字符串。
   - 测试后已恢复原始配置，未保留测试状态。

6. 完成自动化测试回归
   - 已通过：
     - `test/remote_assist_utils_test.dart`
     - `test/remote_assist_health_status_test.dart`
     - `test/runtime_storage_paths_test.dart`

7. 完成制品一致性校验
   - 以下 `vntcrustdesk.msi` 的 SHA256 一致：
     - `D:\Myproject\vnt2.0\vntcrustdesk-src\artifacts\windows\vntcrustdesk.msi`
     - `D:\Myproject\vnt2.0\VntcApp1.0\third_party\vntcrustdesk\windows\dist\vntcrustdesk.msi`
     - `D:\Myproject\vnt2.0\VntcApp1.0\dist\portable\VNT_App_2.3_Windows_Portable\remote_assist\artifacts\vntcrustdesk.msi`
     - `D:\Myproject\vnt2.0\VntcApp1.0\dist\installer\stage\vntcrustdesk.msi`
   - 一致值：
     - `9A8E84D9E88AFEAE320E6B919243B11CC4A7A7C08F2F41F9148BBB21EF1DD424`

## 本次验证结果

- 安装包构建成功。
- 便携包构建成功。
- 安装包 SHA256：
  - `91C18A758BC1C8722D16D3C40493DC1D946ACC089F3DE154C29F27C31542BCC0`
- 便携包 ZIP SHA256：
  - `7FD38793958F8854A019B7F584439C24094CEFEAC46621545514B67B44C23D40`
- `scripts/build_version.txt` 已按规则从 `2.3` 自动推进到 `2.4`。
- Windows 构建期间仅出现既有链接警告 `LNK4042`，未阻断出包。

## 当前涉及模块

- 打包脚本与版本流转
  - `scripts/export_installer_package.ps1`
  - `scripts/export_portable_package.ps1`
  - `scripts/build_windows.bat`
  - `scripts/build_version.txt`
- 远程协助制品
  - `third_party/vntcrustdesk/windows/dist/vntcrustdesk.msi`
- 输出产物
  - `dist/installer/`
  - `dist/portable/`

## 当前状态

- 新版安装包编译：完成
- 远程协助密码链路回归：完成
- 远程协助制品接入校验：完成
- 自动化测试回归：完成
- 双机真实业务联调：未执行

## 当前修改到哪一行 / 哪一个模块

- 本轮未修改业务源码逻辑。
- 本轮停留在打包与验证阶段，涉及模块为：
  - `scripts/build_version.txt` 被编译脚本自动从 `2.3` 推进到 `2.4`
  - `dist/installer/` 与 `dist/portable/` 已生成最新 `2.3` 产物

## 待办清单

1. 在真实目标机器上安装 `VNT_App_2.3_Windows_Setup.exe` 做完整安装链路验证。
2. 准备双机环境验证远程协助核心场景：
   - 设置非空密码后无人值守直连。
   - 清空密码后必须本机点击接受。
   - 在线用户列表仅显示在线节点。
   - 点击在线用户卡片仅回填虚拟 IP。
3. 如安装机环境出现服务权限、UAC、升级覆盖或防火墙问题，再基于本快照继续修正。

## 下一步要做什么

1. 优先拿这次新产出的安装包做实机安装。
2. 再进行双机端到端联调，确认“打包成功”已经真正转化为“业务可用”。

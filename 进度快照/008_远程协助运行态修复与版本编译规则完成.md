# 008 远程协助运行态修复与版本编译规则完成

## 本轮目标

- 继续从 `007` 快照中断点恢复开发
- 修复 `remote_assist_health_service.dart` 的 Dart 语法错误
- 完成“远程协助实际可用但页面仍误报未运行/未监听”的运行态检测修复
- 完成标题版本显示与编译产物版本命名规则

## 已完成内容

### 1. 已完成本轮备份

- 已创建备份：
  - `backup\009_远程协助运行态检测续修前备份_20260601_105413`

### 2. 已修复远程协助运行态检测逻辑

- 已修复 `lib/remote_assist/remote_assist_health_service.dart` 中导致 Dart 无法解析的 PowerShell 字符串问题
- 已将运行态判断从“只认服务状态”扩展为综合判断：
  - Windows 服务状态
  - `vntcrustdesk.exe` 后台进程
  - `--server` 进程
  - `--service` 进程
  - `49999` 监听状态
- 已增强 `ensureBackgroundReady()`：
  1. 先检查当前运行态是否已经可用
  2. 尝试启动服务
  3. 尝试直接拉起 `vntcrustdesk.exe --server`
  4. 最后才走提权安装/修复
- 已在 `lib/remote_assist/remote_assist_launcher.dart` 中保留并接通：
  - `tryStartBackgroundServer()`

### 3. 已完成版本标题与版本规则

- 已新增：
  - `lib/app_version.dart`
  - `scripts/build_version.txt`
  - `编译版本规则.md`
- 左上标题、窗口标题、托盘提示已统一接入版本标题：
  - 当前编译出的标题为：`VNT App 2.0 v2.1`
- 编译版本规则已接入：
  - `scripts/build_windows.bat`
  - `scripts/export_portable_package.ps1`
  - `scripts/export_installer_package.ps1`

### 4. 已完成构建与产物验证

- 已通过 Windows 构建：
  - `cmd /d /c scripts\build_windows.bat`
- 已验证新构建主程序的版本资源：
  - `FileVersion = 2.1.0`
  - `ProductVersion = 2.1.0`
- 已实际启动新构建程序并确认主窗口标题：
  - `VNT App 2.0 v2.1`
- 已导出便携包并确认文件名：
  - `dist\portable\VNT_App_2.1_Windows_Portable.zip`
- 已导出安装包并确认文件名：
  - `dist\installer\VNT_App_2.1_Windows_Setup.exe`
- 已验证自动推进规则：
  - 当前产物版本是 `2.1`
  - `scripts/build_version.txt` 已自动推进为下一次编译使用的 `2.2`

### 5. 已完成远程协助底层健康验证

- 已在当前机器按新逻辑核对到：
  - `ServiceExists = true`
  - `ServiceState = Running`
  - `HasServerProcess = true`
  - `HasServiceProcess = true`
  - `PortListening = true`
- 说明：
  - 当前机器在新逻辑下会被判定为远程协助后台健康，不应再落入“服务未运行 / 49999 未监听”的旧误判路径

### 6. 已完成代码与脚本验证

- 已通过：
  - `dart format`（Dart 文件）
  - `flutter analyze lib\remote_assist lib\app_version.dart lib\main.dart lib\system_tray_manager.dart lib\widgets\custom_title_bar.dart`
  - `flutter test test\remote_assist_utils_test.dart`
  - `flutter test test\remote_assist_health_status_test.dart`
- 已通过 PowerShell 语法解析：
  - `scripts\export_portable_package.ps1`
  - `scripts\export_installer_package.ps1`
  - `scripts\bootstrap_vntcrustdesk.ps1`
  - `scripts\uninstall_vntcrustdesk.ps1`
- `flutter analyze` 当前仅剩信息级提示：
  - `lib/main.dart` 的 `onPopInvoked` 已弃用提示
  - `lib/widgets/custom_title_bar.dart` 的 `withOpacity` 已弃用提示
- 这些不是本轮功能阻塞

## 当前修改到哪一行 / 哪一个模块

- `lib/remote_assist/remote_assist_health_service.dart`
  - 运行态聚合判断：
    - `57`
    - `67`
  - `ensureBackgroundReady()`：
    - `156`
  - `_isRuntimeReady(...)`：
    - `211`
  - `_queryRuntimeProcesses(...)`：
    - `359`
  - `_isPortListening(...)`：
    - `425`
- `lib/remote_assist/remote_assist_launcher.dart`
  - `tryStartBackgroundServer()`：
    - `174`
- `lib/app_version.dart`
  - 统一标题/版本注入：
    - `1`
- `lib/main.dart`
  - 窗口标题 / 托盘标题接入：
    - `214`
    - `240`
    - `803-804`
- `lib/system_tray_manager.dart`
  - 托盘提示版本化：
    - `157`
    - `168`
- `lib/widgets/custom_title_bar.dart`
  - 左上标题版本显示：
    - `146`
- `scripts/build_windows.bat`
  - 编译版本读取与 `dart-define` 注入：
    - `22-32`
    - `50`
- `scripts/export_portable_package.ps1`
  - 当前版本读取 / 验证模式 / 成功后版本推进：
    - `2`
    - `97-105`
    - `124-128`
    - `192-193`
- `scripts/export_installer_package.ps1`
  - 复用当前版本导出安装包 / 导出成功后推进下一版本：
    - `151-159`
    - `183-190`
    - `291`
- `scripts/build_version.txt`
  - 当前下一次编译版本：
    - `1`
- `编译版本规则.md`
  - 版本规则文档：
    - `1`

## 待办清单（未完成）

- 让用户在双机环境继续做最终实测：
  - 打开本轮新的便携包或安装包
  - 进入远程协助页面
  - 确认页面状态是否恢复为：
    - `服务运行中`
    - `49999 监听中`
- 继续做双机真实远控回归：
  - 手输对端虚拟 IP
  - 对端同意邀请
  - 观察页面状态是否稳定刷新

## 下一步要做什么

1. 让用户优先使用本轮产物：
   - `dist\portable\VNT_App_2.1_Windows_Portable.zip`
   - `dist\installer\VNT_App_2.1_Windows_Setup.exe`
2. 在目标环境重新打开远程协助页面，确认旧误报是否消失
3. 如果还有个别机器误报，再根据那台机器的 `remote_assist.log` 做最后一轮差异化修复

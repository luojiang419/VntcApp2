# 007 远程协助运行态检测与版本规则开发中断点

## 本轮目标

- 修复“远程协助实际可连通，但 VNTC 客户端仍提示服务未运行、49999 未监听”的状态误判/后台拉起问题
- 在左上标题后追加显示版本号，当前目标版本为 `v2.1`
- 建立后续编译时文件名与标题版本自动 `+0.1` 的规则

## 已完成内容

### 1. 已完成本轮备份

- 已创建备份：
  - `backup\008_远程协助通信检测与版本规则前备份_20260601_103948`

### 2. 已完成远程协助运行态检测改造的主体代码

- 已在 `lib/remote_assist/remote_assist_health_service.dart` 中开始把健康检查从“只认 Windows 服务”改为“认真实后台运行态”：
  - 新增运行时进程查询 `_queryRuntimeProcesses(...)`
  - `collectStatus(...)` 现在会同时综合：
    - 服务状态
    - `vntcrustdesk.exe` 进程状态
    - `--server / --service` 后台进程
    - `49999` 监听状态
- 已调整 `ensureBackgroundReady()` 的兜底顺序：
  1. 先判断当前运行态是否已经可用
  2. 尝试启动 `vntcrustdesk` 服务
  3. 尝试直接拉起 `vntcrustdesk.exe --server`
  4. 最后才走提权安装/修复
- 已在 `lib/remote_assist/remote_assist_launcher.dart` 中新增：
  - `tryStartBackgroundServer()`

### 3. 已完成版本显示抽离

- 已新增 `lib/app_version.dart`
- 已把以下位置切到统一版本标题来源：
  - `lib/widgets/custom_title_bar.dart`
  - `lib/main.dart`
  - `lib/system_tray_manager.dart`
- 当前默认标题显示来源已切为：
  - `VNT App 2.0 v2.1`

### 4. 已完成编译版本规则接入

- 已新增版本源文件：
  - `scripts/build_version.txt`
  - 当前内容为：`2.1`
- 已改造：
  - `scripts/build_windows.bat`
  - `scripts/export_portable_package.ps1`
  - `scripts/export_installer_package.ps1`
- 目标规则已写入：
  - `编译版本规则.md`

### 5. 已完成本轮脚本级验证

- 已通过 PowerShell 语法解析：
  - `scripts/export_portable_package.ps1`
  - `scripts/export_installer_package.ps1`
  - `scripts/bootstrap_vntcrustdesk.ps1`
  - `scripts/uninstall_vntcrustdesk.ps1`

## 当前修改到哪一行 / 哪一个模块

- `lib/remote_assist/remote_assist_health_service.dart`
  - `collectStatus(...)` 运行态合并判断：
    - `57`
    - `67`
    - `155`
  - `_isRuntimeReady(...)`：
    - `209`
  - `_queryRuntimeProcesses(...)`：
    - `357`
  - `_isPortListening(...)`：
    - `423`
- `lib/remote_assist/remote_assist_launcher.dart`
  - `tryStartBackgroundServer()`：
    - `174`
- `lib/app_version.dart`
  - 统一标题/版本常量：
    - `1`
- `lib/main.dart`
  - 窗口标题与托盘标题接入：
    - `214`
    - `240`
    - `803-804`
- `lib/system_tray_manager.dart`
  - 托盘 tooltip 版本化：
    - `157`
    - `168`
- `lib/widgets/custom_title_bar.dart`
  - 左上标题版本显示：
    - `146`
- `scripts/build_windows.bat`
  - 编译版本文件读取与 `dart-define` 注入：
    - `7`
    - `22-32`
    - `50`
- `scripts/export_portable_package.ps1`
  - 便携包版本读取与成功后自动 `+0.1`：
    - `9`
    - `97-105`
    - `124-128`
    - `193`
- `scripts/export_installer_package.ps1`
  - 安装包版本读取、复用便携包版本、成功后自动 `+0.1`：
    - `5`
    - `151-159`
    - `183-188`
    - `291`
- `编译版本规则.md`
  - 规则文档：
    - `1`

## 当前未完成 / 明确阻塞

- `lib/remote_assist/remote_assist_health_service.dart` 目前存在 Dart 语法错误，尚未完成本轮格式化与测试
- 已定位到本轮中断点为：
  - `lib/remote_assist/remote_assist_health_service.dart`
  - `396`
  - `397`
- 现象：
  - 嵌入 PowerShell 字符串里的正则文本导致 Dart 解析失败
  - `dart format` 在此处中断

## 本轮尚未完成的验证

- 未完成 `dart format` 全量通过
- 未完成 `flutter analyze`
- 未完成：
  - `flutter test test/remote_assist_utils_test.dart`
  - `flutter test test/remote_assist_health_status_test.dart`
- 未完成安装包/便携包实际导出验证
- 未完成双机远程协助实测回归

## 待办清单（未完成）

1. 修复 `remote_assist_health_service.dart` 第 `396-397` 行附近的 Dart 字符串转义问题
2. 重新执行 `dart format`
3. 执行 `flutter analyze`
4. 执行远程协助相关测试
5. 重新导出便携包/安装包，确认：
   - 文件名版本变为当前编译版本
   - 软件左上标题显示同一版本
6. 双机验证页面状态是否恢复为：
   - `服务运行中`
   - `49999 监听中`

## 下一步要做什么

1. 先修正 `lib/remote_assist/remote_assist_health_service.dart` 中 PowerShell 正则字符串的 Dart 转义写法
2. 让 `dart format` 与 `flutter analyze` 先恢复通过
3. 再做一次远程协助状态页与版本规则的完整编译验证

# 012 本机状态迁移到链接状态页完成

## 本轮目标

- 将“本机状态”从远程协助页面迁移到“链接状态”页面
- 改为默认折叠展示，保留展开后的详细状态与安装/修复入口
- 保持远程协助页面只负责连接目标与选择在线用户

## 已完成内容

### 1. 已完成本轮备份

- 已创建备份：
  - `backup\017_本机状态迁移前备份_20260601_124506`
- 已按规则清理旧备份，仅保留最近 10 份

### 2. 已完成页面职责迁移

- 已从远程协助页移除“本机状态”卡片
- 已将同等状态信息迁移到“链接状态”页头部下方
- 新状态面板默认折叠，仅展示：
  - 状态指示图标
  - 状态文案
  - 展开/收起箭头

### 3. 已完成展开态详细信息接入

- 展开后可查看：
  - VNT 连接状态
  - `vntcrustdesk` 安装状态
  - 服务运行状态
  - `49999` 监听状态
  - Presence 状态
  - 本机虚拟 IP
  - 虚拟网段
  - 运行时版本
  - 安装方式
  - 修复能力
  - 防火墙规则
  - 管理员权限
  - 可执行文件路径
  - 问题列表
- “安装/修复”按钮已一起迁移，仍通过 `RemoteAssistManager.repair()` 工作

### 4. 已完成远程协助页文案收敛

- 远程协助页说明文案已改为：
  - “支持访问密码无人值守或手动接受”
- 当前远程协助页只保留：
  - 页面头部
  - 目标连接卡片
  - 在线用户列表

## 当前修改到哪一行 / 哪一个模块

- `lib/pages/remote_assist_page.dart`
  - 页面说明文案更新：
    - `136`
  - “本机状态”卡片已移除，页面主体只保留连接与用户列表：
    - `83-91`
- `lib/pages/link_status_page.dart`
  - 远程协助状态管理接入：
    - `31`
    - `55-56`
  - 折叠面板挂载到 Header 下方：
    - `133-150`
  - 远程协助状态面板主体：
    - `330-539`

## 验证结果

- 已通过：
  - `D:\APPdata\flutter\bin\dart.bat analyze lib\pages\remote_assist_page.dart lib\pages\link_status_page.dart`
- 当前仅剩仓库内原有的 `info` 级提示：
  - `withOpacity` 弃用提示
  - 个别 `prefer_const_constructors` / `unnecessary_to_list_in_spreads`
- 本轮未引入新的错误级或警告级问题

## 待办清单（未完成）

1. 增加“设置远程密码”按钮与弹窗
2. 打通主项目到 `vntcrustdesk-src` 的远程密码配置链路
3. 去掉 `vntcrustdesk` 自身密码设置页的复杂度限制
4. 远程协助页在线用户过滤离线用户
5. 目标连接拆分为“虚拟IP + 访问密码”双输入框
6. 补充测试并做密码场景联调

## 下一步要做什么

1. 先创建下一轮备份
2. 实现“设置远程密码”按钮与弹窗
3. 给 `RemoteAssistManager / RemoteAssistLauncher` 增加密码配置动作
4. 同步修改 `vntcrustdesk-src`，提供外部可调用的无界面密码/批准模式配置入口

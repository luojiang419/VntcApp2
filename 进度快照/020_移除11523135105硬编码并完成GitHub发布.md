# 进度快照 020 - 移除 115.231.35.105 硬编码并完成 GitHub 发布

## 本次已完成

1. 完成安全红线清理
   - 已移除应用默认配置中的 `115.231.35.105` 服务器硬编码预设。
   - 已同步清理测试样例与辅助生成脚本中的同地址内容。
   - 当前应用新建配置时不再自动填入任何服务器地址。

2. 完成源码仓库清理边界
   - `.gitignore` 已新增排除：
     - `backup/`
     - `temp/`
     - `configure-access-password/`
     - `vntcrustdesk-src/`
   - 这样可避免把本地备份、测试日志和独立 fork 工作树误推到主仓库。

3. 完成新版本重新编译
   - 已重新编译 `2.5` 安装包与便携包。
   - 软件显示名称、EXE 元数据同步为 `VNT App 2.5`。
   - `scripts/build_version.txt` 已自动推进到下一次版本 `2.6`。

4. 完成 GitHub 仓库创建与源码推送
   - 已创建私有仓库：
     - `https://github.com/luojiang419/VntcApp2.0`
   - 已推送 `main` 分支源码基线。

5. 完成 GitHub Release 发布
   - 已创建发布：
     - `https://github.com/luojiang419/VntcApp2.0/releases/tag/v2.5`
   - 已上传资产：
     - `VNT_App_2.5_Windows_Setup.exe`
     - `VNT_App_2.5_Windows_Setup.sha256`
     - `VNT_App_2.5_Windows_Portable.zip`
     - `VNT_App_2.5_Windows_SHA256.txt`

## 本次验证结果

- 已验证源码目录无残留：
  - `rg -n "115\.231\.35\.105|11523135105" -S .`
  - 当前返回为空
- 已验证构建与发布产物无残留：
  - `rg -a -n "115\.231\.35\.105|11523135105" build output dist lib scripts testdata windows linux macos ios android web -S`
  - 当前返回为空
- 已验证安装包构建成功：
  - `PowerShell -ExecutionPolicy Bypass -File scripts/export_installer_package.ps1`
- 已验证 Windows EXE 元数据：
  - `FileDescription = VNT App 2.5`
  - `ProductName = VNT App 2.5`
  - `FileVersion = 2.5.0`
  - `ProductVersion = 2.5.0`
- 已验证 GitHub 侧结果：
  - 仓库存在，默认分支为 `main`
  - 仓库可见性为 `PRIVATE`
  - `v2.5` Release 资产上传完成

## 当前产物

- 安装包：
  - `dist/installer/VNT_App_2.5_Windows_Setup.exe`
- 安装包校验：
  - `dist/installer/VNT_App_2.5_Windows_Setup.sha256`
- 便携包：
  - `dist/portable/VNT_App_2.5_Windows_Portable.zip`
- 便携包校验：
  - `dist/portable/VNT_App_2.5_Windows_SHA256.txt`

## 本轮备份

- 备份目录：
  - `backup/011_移除11523135105硬编码与GitHub发布前备份_20260601_224103`

## 当前状态

- 115.231.35.105 硬编码预设：已移除
- 源码与产物残留扫描：已通过
- `2.5` 安装包编译：已完成
- GitHub 仓库创建：已完成
- GitHub Release 发布：已完成

## 下一步要做什么

1. 如果需要对外公开源码，可把 `VntcApp2.0` 仓库从私有改为公开。
2. 如果需要继续发布下一版，当前待编译版本号已经推进到 `2.6`。
3. 如果后续还要清理更多安全红线，建议下一轮再做一次更广的密钥/地址类静态扫描。

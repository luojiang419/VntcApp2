# 049-路径测试跨平台断言修复

## 时间
- 2026-07-09

## 本次目标
- 修复全量测试中 3 个路径分隔符断言失败。
- 仅调整测试断言，不改业务路径解析逻辑。

## 已完成内容

### 1. 已创建阶段备份
- 新增：`backup/003-路径测试跨平台断言修复前.md`

### 2. 已修复路径比较方式
- `test/remote_assist_macos_runtime_test.dart`
  - 将 macOS runtime app/executable 路径断言改为统一分隔符后比较。
- `test/runtime_storage_paths_test.dart`
  - 将 macOS bundle resources 路径断言改为统一分隔符后比较。

## 当前修改到哪个模块
- 当前完成模块：
  - `模块4：路径测试跨平台断言修复`

## 具体修改的代码前后对比

修改前：

```dart
expect(runtime.appBundlePath, path.normalize(runtimeApp));
expect(runtime.executablePath, runtimeExecutable);
```

修改后：

```dart
expect(_portablePath(runtime.appBundlePath), _portablePath(runtimeApp));
expect(_portablePath(runtime.executablePath), _portablePath(runtimeExecutable));

String _portablePath(String value) =>
    path.normalize(value).replaceAll(r'\', '/');
```

## 验证结果
- 通过：`D:\flutter\bin\dart.bat format test\remote_assist_macos_runtime_test.dart test\runtime_storage_paths_test.dart`
- 通过：`D:\flutter\bin\flutter.bat test test\remote_assist_macos_runtime_test.dart test\runtime_storage_paths_test.dart -r expanded`
  - 结果：8 项测试通过。

## 待办清单（未完成）
- 重跑全量 `flutter test`。
- 记录全量 `flutter analyze` 既有 lint 状态。
- 尝试 Windows release 构建或安装包构建。
- 初始化 git 并推送源码。
- 清理临时缓存。

## 下一步要做什么
- 重跑全量测试，确认本次失败已消除。

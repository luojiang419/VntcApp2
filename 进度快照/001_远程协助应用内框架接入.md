# 001 远程协助应用内框架接入

## 已完成内容
- 新增远程协助应用内模块：`lib/remote_assist/`
- 新增远程协助页面：`lib/pages/remote_assist_page.dart`
- 在主导航中接入“远程协助”菜单，并加入页面槽位
- 新增 `RemoteAssistManager / Launcher / HealthService / PresenceService / Log`
- Presence 使用 UDP `49998` 做轻量在线名称补齐，叠加现有 VNT 在线设备列表展示
- 连接入口统一为 `vntcrustdesk`，端口固定为 `49999`
- 已移除旧的 runtime/便携/内嵌兜底路径，不沿用旧方案
- 新增单元测试：`test/remote_assist_utils_test.dart`
- 已通过：`flutter test test/remote_assist_utils_test.dart`
- 已通过：`dart analyze` 对本次新增远程协助模块的错误级检查（仅剩项目内既有/信息级提示）

## 当前修改到哪一行/哪一个模块
- 远程协助核心实现集中在 `lib/remote_assist/`
- 页面入口已接到 `lib/pages/main_navigation_shell.dart`
- 全局生命周期接入已写入 `lib/main.dart`
- 当前进行到“安装器与 MSI 集成”前，应用内框架已完成

## 待办清单（未完成）
- 接入 `vntcrustdesk.msi` 的安装/bootstrap/卸载脚本
- 修改 Inno 安装器，在主安装流程中静默安装 `vntcrustdesk`
- 生成并消费 `vntcrustdesk_manifest.json`
- 约定外部独立 fork 产物目录与当前仓库消费路径
- 落盘 `远程协助功能开发.md` 正式开发文档
- 跑安装器链路相关验证

## 下一步要做什么
- 先执行第 002 次源码备份
- 开始改 `scripts/export_installer_package.ps1` 及新增 PowerShell 安装脚本
- 完成 `vntcrustdesk` 受管安装、manifest 写入、卸载保护与文档落盘

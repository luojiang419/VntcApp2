# 002 远程协助安装器与 MSI 接入

## 已完成内容
- 远程协助入口已经接入主导航，应用内页面和管理器已正常落地
- 远程协助连接链路已固定为 `vntcrustdesk` + `49999`，不再保留旧 runtime/便携/内嵌兜底方案
- 新增 `scripts/bootstrap_vntcrustdesk.ps1`
- 新增 `scripts/uninstall_vntcrustdesk.ps1`
- 新增 `scripts/stage_vntcrustdesk_artifact.ps1`
- 修改 `scripts/export_installer_package.ps1`，主安装包现在会强制携带 `vntcrustdesk.msi` 与 bootstrap/uninstall 脚本
- 新增 `third_party/vntcrustdesk/windows/README.md`，明确当前仓库只消费独立 fork 产物
- 新增 `远程协助功能开发.md`，记录当前新方案、验证结论和剩余阻塞
- 已修复 PowerShell 脚本 `param(...)` 顺序问题，避免脚本真实执行时解析异常
- 已通过 `bootstrap / uninstall / stage / export_installer_package` 四个脚本的 PowerShell 语法解析
- 已通过 `scripts/stage_vntcrustdesk_artifact.ps1` 最小烟测，可实际复制 MSI 产物并生成版本清单
- 已通过：`flutter test test/remote_assist_utils_test.dart`
- 已通过：远程协助相关 `dart analyze` 错误级检查（仅剩项目现有 info 级弃用提示）

## 当前修改到哪一行/哪一个模块
- 应用内实现集中在 `lib/remote_assist/` 与 `lib/pages/remote_assist_page.dart`
- 安装器链路集中在 `scripts/export_installer_package.ps1`
- MSI 安装、卸载、分发脚本集中在 `scripts/bootstrap_vntcrustdesk.ps1`、`scripts/uninstall_vntcrustdesk.ps1`、`scripts/stage_vntcrustdesk_artifact.ps1`
- 外部产物约定集中在 `third_party/vntcrustdesk/windows/`

## 待办清单（未完成）
- 在独立 `vntcrustdesk` fork 仓库真正产出可安装的 `vntcrustdesk.msi`
- 将真实 MSI 放入 `third_party/vntcrustdesk/windows/dist/`
- 基于真实 MSI 导出主安装包
- 做双机实装验证、官方 RustDesk 共存验证、49999 监听验证
- 做受控确认、UAC/高权限窗口、重启后服务恢复等端到端验证

## 下一步要做什么
- 先在独立 fork 仓库完成 `vntcrustdesk` 的源码改造和 MSI 打包
- 回到当前仓库执行 `scripts/stage_vntcrustdesk_artifact.ps1`
- 再执行 `scripts/export_installer_package.ps1` 生成主安装包
- 最后做真实安装包联调，而不是继续沿用旧方案做本地兜底

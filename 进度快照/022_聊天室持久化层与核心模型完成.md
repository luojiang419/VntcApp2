# 进度快照 022 - 聊天室持久化层与核心模型完成

## 本次已完成

1. 已新增聊天室核心目录
   - `lib/chat/chat_constants.dart`
   - `lib/chat/chat_models.dart`
   - `lib/chat/chat_storage.dart`
   - `lib/chat/chat_manager.dart`

2. 已完成本地持久化基础设施
   - SQLite 数据库路径固定为 `config/chat/chat.db`
   - 附件目录固定为 `config/chat/attachments/`
   - 已建表：
     - `conversations`
     - `messages`
     - `attachments`
     - `room_membership_cache`
     - `sync_checkpoints`

3. 已完成核心模型定义
   - 会话模型
   - 消息模型
   - 附件模型
   - 房间模型
   - Presence / 历史补同步传输模型

4. 已完成最小聊天管理器骨架
   - 可初始化存储
   - 可从存储重载会话、房间、未读
   - 可选择大厅、选择会话、读取消息、标记私聊已读

5. 已完成第二步验证
   - 静态检查：
     - `D:\APPdata\flutter\bin\dart.bat analyze lib\chat\chat_models.dart lib\chat\chat_storage.dart lib\chat\chat_manager.dart test\chat_storage_test.dart`
     - 结果：`No issues found!`
   - 单元测试：
     - `D:\APPdata\flutter\bin\flutter.bat test test\chat_storage_test.dart`
     - 结果：`All tests passed!`

## 当前修改位置

- 持久化模块：
  - `lib/chat/chat_storage.dart`
- 聊天数据模型模块：
  - `lib/chat/chat_models.dart`
- 管理器骨架模块：
  - `lib/chat/chat_manager.dart`
- 持久化验证测试：
  - `test/chat_storage_test.dart`

## 待办清单

1. 实现 VNT 在线节点到聊天室 Presence 的发现链路
2. 生成大厅卡片与当前大厅在线用户列表
3. 实现大厅 / 私聊 / 房间文本消息发送接收
4. 接入未读数与会话切换逻辑到真实 UI
5. 实现断网恢复后的历史补同步
6. 实现图片 / 视频 / 文件 / 语音消息
7. 实现 Windows 防火墙同步

## 下一步要做什么

下一步进入第三阶段：把 VNT 在线设备、聊天室 Presence 和大厅/房间状态串起来，并先打通文本消息与私聊未读链路，让页面从骨架变成真正可聊天的页面。

# 014 远程协助在线用户过滤完成

## 本轮目标

- 让远程协助页“在线用户”栏只显示在线目标
- 不影响链接状态页原有在线/离线设备分组

## 已完成内容

### 1. 已完成本轮备份

- 已创建备份：
  - `backup\019_在线用户过滤前备份_20260601_125636`
- 已继续按规则只保留最近 10 份备份

### 2. 已完成远程协助页在线过滤

- 远程协助页现在会先对 `_manager.peers` 做在线筛选
- 当前只渲染：
  - `peer.isOnline == true` 的目标
- 离线用户虽然仍保留在管理层数据中，但不会再显示在远程协助页列表里

### 3. 已完成空状态文案更新

- 当没有在线目标时，页面提示已改为：
  - “当前没有在线的远程协助目标”
- 文案同时补充说明：
  - 需要对端在线
  - 需要对端已安装 `vntcrustdesk`

## 当前修改到哪一行 / 哪一个模块

- `lib/pages/remote_assist_page.dart`
  - 在线用户筛选：
    - `149`
  - 无在线目标提示文案：
    - `327`

## 验证结果

- 已通过：
  - `D:\APPdata\flutter\bin\dart.bat analyze lib\pages\remote_assist_page.dart`
- 当前未引入新的错误级或警告级问题
- 链接状态页未做改动，因此原有在线/离线分组逻辑不受影响

## 待办清单（未完成）

1. 目标连接拆分为“虚拟IP + 访问密码”
2. `RemoteAssistManager.launchController(...)` 接入会话密码参数
3. `RemoteAssistLauncher.openRemoteDesktop(...)` 追加 `--password`
4. 重新构建 `vntcrustdesk` 产物并做端到端验证

## 下一步要做什么

1. 先创建下一轮备份
2. 改造远程协助页连接卡片为双输入框
3. 把访问密码透传到 `vntcrustdesk` 会话启动参数
4. 做主项目与 `vntcrustdesk-src` 的统一构建验证

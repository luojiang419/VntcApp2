# 进度快照 019 - 软件名称与版本号同步编译完成

## 本次已完成

1. 完成显示名称与版本号同步
   - Flutter 运行时显示名称改为随构建版本变化。
   - Windows 窗口标题、`MaterialApp` 标题、Linux 自启动名称已改为读取统一版本信息。
   - 编译脚本新增 `APP_PRODUCT_NAME` 注入，当前构建产物显示名称为 `VNT App 2.4`。

2. 完成 Windows 可执行文件元数据同步
   - `windows/runner/Runner.rc`
   - `windows/runner/main.cpp`
   - 以上位置不再写死 `VNT App 2.0`，而是根据 `FLUTTER_VERSION_MAJOR` / `FLUTTER_VERSION_MINOR` 生成显示名称。

3. 完成安装器显示名称同步
   - 安装器 `AppName`、`AppVerName`、开始菜单快捷方式名、桌面快捷方式名已改为 `VNT App 2.4`。
   - 同时保留安装目录与程序组基名为 `VNT App`，避免每次升级切换安装路径。

4. 完成版本规则文档同步
   - `编译版本规则.md` 已更新为新的命名规则。
   - 现在文档明确说明“软件显示名称”会跟随 `scripts/build_version.txt` 当前版本号变化。

5. 明确保留内部稳定标识
   - `remote_assist` 的 `managedBy`
   - Windows 运行时数据目录名
   - 上述内部标识仍保持 `VNT App 2.0`，避免影响升级、托管识别和已有数据路径。

## 本次验证结果

- 已通过：
  - `PowerShell -ExecutionPolicy Bypass -File scripts/export_installer_package.ps1`
- 已验证：
  - 新安装包产物：`dist/installer/VNT_App_2.4_Windows_Setup.exe`
  - 新便携包产物：`dist/portable/VNT_App_2.4_Windows_Portable.zip`
- 已验证：
  - `build/windows/x64/runner/Release/vnt_app.exe` 文件元数据
  - `FileDescription = VNT App 2.4`
  - `ProductName = VNT App 2.4`
  - `FileVersion = 2.4.0`
  - `ProductVersion = 2.4.0`
- 已验证：
  - 安装器阶段文件 `dist/installer/stage/VNT_App_2.4_Windows_Setup.iss`
  - `MyAppName = VNT App 2.4`
  - `DefaultDirName = {autopf}\VNT App`
- 已验证：
  - `scripts/build_version.txt` 已自动推进到下一次版本 `2.5`

## 当前产物

- 安装包：
  - `dist/installer/VNT_App_2.4_Windows_Setup.exe`
- 安装包校验：
  - `dist/installer/VNT_App_2.4_Windows_Setup.sha256`
- 便携包目录：
  - `dist/portable/VNT_App_2.4_Windows_Portable`
- 便携包压缩包：
  - `dist/portable/VNT_App_2.4_Windows_Portable.zip`
- 便携包校验：
  - `dist/portable/VNT_App_2.4_Windows_SHA256.txt`

## 当前涉及模块

- 版本信息
  - `lib/app_version.dart`
- 主入口与窗口标题
  - `lib/main.dart`
- Linux 自启动名称
  - `lib/pages/settings_page.dart`
- Windows 构建脚本
  - `scripts/build_windows.bat`
- 安装器导出脚本
  - `scripts/export_installer_package.ps1`
- Windows 原生显示名
  - `windows/runner/Runner.rc`
  - `windows/runner/main.cpp`
- 版本规则文档
  - `编译版本规则.md`

## 当前状态

- 显示名称跟随版本号：完成
- 安装器显示名称跟随版本号：完成
- Windows EXE 元数据同步：完成
- `2.4` 安装包编译：完成
- 下一个待编译版本推进到 `2.5`：完成
- 安装后实际界面手工点验：待执行

## 下一步要做什么

1. 安装 `VNT_App_2.4_Windows_Setup.exe`，确认安装器界面、桌面快捷方式和开始菜单显示名都是 `VNT App 2.4`。
2. 启动安装版后确认主窗口标题显示为 `VNT App 2.4`。
3. 如果后续希望连内部托管标识也去掉 `2.0`，需要单独做一轮兼容性评估后再改。
